# --- Core: model scoring and analysis ---
torch
transformers>=4.45,<4.50
accelerate
pandas
numpy
scipy              # kendalltau / spearmanr in persona_analysis
scikit-learn       # sklearn.manifold.MDS in analysis_support.py
tqdm               # progress bars in the generate_* scripts
pyyaml
python-dotenv

# --- Word embeddings ---
# word2vec / GloVe vectors for vectorAnalysis.ipynb and topTokenSemantics.ipynb.
# 4.3.3 is the first release that imports cleanly against scipy >= 1.13, which
# removed scipy.linalg.triu.
gensim>=4.3.3

# --- Figures and tables ---
matplotlib
seaborn
adjustText         # outlier-label repulsion in personaFigures/gen_figures.ipynb
dataframe_image    # PNG export of styled tables (personaFigures)
ipython            # display() in personaFigures/gen_figures.ipynb

# --- Dictionary labelling ---
# Only needed to regenerate data/dictionaries/mfd2.0_typed.csv and re-draw the
# `sampled` substitutions. Both outputs are committed, so a run of the scoring
# pipeline (including the Colab notebooks) does not need this.
nltk               # WordNet lexicon in generate_typed_dictionary.py; also needs
                   # the corpus: python -c "import nltk; nltk.download('wordnet')"
